version: "0.0.1"

metadata:
  name: "discussion-summariser-flow"
  description: "A summary of your discussion"
  author: "digit"
  tags: [discussion-summariser]
  private: true

inputs:
  discussion:
    type: string
    description: "the transcript of the discussion"
    required: true
    example: "Example"

model:
  provider: "openai"
  name: "gpt-4o"

prompt: |
  following is a discussion between a team. using its context, generate a conclusive summary of the same
  create a JSON file format 'feature' : 'value or description' for the same, make suree the key 'title' storoes the feature title. the ouput should only contain the JSON and the summary.
  {discussion}

readme: |
  It's a discussion summariser.